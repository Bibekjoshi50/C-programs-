#include<stdio.h>
int main(){
	int integer;
	float decimal;
	double doub;
	char character;
	 printf("\nThe size of int is %d bytes",sizeof(integer));
	 printf("\nThe size of float  is %d bytes",sizeof(decimal));
	 printf("\nThe size of double is %d bytes",sizeof(doub));
	 printf("\nThe size of char is %d bytes",sizeof(character));
}
